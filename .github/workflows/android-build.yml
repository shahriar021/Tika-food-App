name: CI/CD Android Build

on:
  push:
    branches: [ main ]  # Trigger on main branch

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      EAS_TOKEN: ${{ secrets.EXPO_TOKEN }}   # ← Use your existing token

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Install EAS CLI
      - name: Install EAS CLI
        run: npm install -g eas-cli@latest

      # 4️⃣ Install NPM dependencies
      - name: Install dependencies
        run: npm install

      # 5️⃣ Prebuild Android folder (Expo)
      - name: Generate Native Android Folder
        run: npx expo prebuild --platform android

      # 6️⃣ Setup Java for Android build
      - name: Setup Java (Android Engine)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # 7️⃣ Decode Android keystore from GitHub secret
      - name: Decode Keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android.jks

      # 8️⃣ Configure EAS Android credentials
      - name: Configure EAS credentials
        run: |
          eas credentials:android --non-interactive \
            --keystore-path android.jks \
            --keystore-password ${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
            --key-alias ${{ secrets.ANDROID_KEY_ALIAS }} \
            --key-password ${{ secrets.ANDROID_KEY_PASSWORD }}

      # 9️⃣ Build signed release APK with EAS
      - name: Build Release APK
        run: eas build --platform android --profile production --non-interactive

      # 🔟 Install Firebase CLI
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      # 1️⃣1️⃣ Create Firebase credentials file
      - name: Create Firebase Credentials File
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > firebase-key.json

      # 1️⃣2️⃣ Export Google credentials for Firebase CLI
      - name: Export Google credentials
        run: echo "GOOGLE_APPLICATION_CREDENTIALS=firebase-key.json" >> $GITHUB_ENV

      # 1️⃣3️⃣ Distribute APK to Firebase App Distribution
      - name: Distribute APK to Firebase
        run: |
          APK_PATH=$(ls ./dist/*.apk | head -n 1)
          firebase appdistribution:distribute $APK_PATH \
          --app 1:773349551035:android:152ae5a7db9ef1aff3729e \
          --groups "testers"
